<!DOCTYPE html>
<html>
  <head>
    <title>CollectorSwap</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    
    <!-- Agrega el enlace a la hoja de estilo de Bootstrap CSS -->
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" %>
    
    <!-- FontAwesome CDN -->
    <link href="https://use.fontawesome.com/releases/v5.15.4/css/all.css" rel="stylesheet">

    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if flash[:notice] %>
      <div class="alert alert-success alert-transparent">
        <%= flash[:notice] %>
      </div>
    <% end %>

    <% if flash[:alert] %>
      <div class="alert alert-danger alert-transparent">
        <%= flash[:alert] %>
      </div>
    <% end %>

    <%= yield %>

    <!-- Agrega los scripts de Bootstrap JavaScript al final del cuerpo -->
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" %>

    <script>
      document.addEventListener('DOMContentLoaded', (event) => {
        setTimeout(() => {
          let alerts = document.querySelectorAll('.alert');
          alerts.forEach(alert => {
            alert.style.display = 'none';
          });
        }, 3000); // 3000 milisegundos = 3 segundos
      });
    </script>
  </body>
</html>


<script>
  document.addEventListener('DOMContentLoaded', (event) => {
    setTimeout(() => {
      let alerts = document.querySelectorAll('.alert');
      alerts.forEach(alert => {
        alert.style.display = 'none';
      });
    }, 3000); // 3000 milisegundos = 3 segundos
  });
</script>
