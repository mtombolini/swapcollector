<h1>Products#index</h1>
<p>Find me in app/views/products/index.html.erb</p>
<%= @products.each do |product| 
end %>


<%# 
<main class="container">
  <h1 class="mt-3">Inicio</h1>
  <hr>

  <div class="row">
    <% @products.each do |product| %>
      <%# <div class="col-md-4 mb-4">
        <h5 class="text-center">
          <%= link_to product.name, product, class: "text-decoration-none" %>
        <%# </h5>

        <hr>

        <div class="card">
          <%= image_tag product.image.variant(resize_to_limit: [150, 150]), class: "card-img-top img-fluid" %>

          <%# <% if user_signed_in? && current_user == product.user %>
            <%# <div class="card-body text-center">
              <%= link_to edit_product_path(product), class: "text-primary" do %>
                <%# <i class="fas fa-pencil-alt"></i>
              <% end %>
              <%# <%= button_to product_path(product), method: :delete, form: { "data-turbo-confirm": "¿Estás seguro?" }, class: "btn btn-link p-0 text-danger" do %>
                <%# <i class="fas fa-trash"></i>
              <% end %>
            <%# </div> %>
          <%# <% end %> 
        <%# </div> %>
      <%# </div>
    <% end %>
  <%# </div> %>
<%# </main>  %>
